# WhatsApp Multi-Sesión 🚀

**WhatsApp Multi-Sesión** es una herramienta para gestionar múltiples sesiones de **WhatsApp Web** de forma sencilla y eficaz, permitiéndote generar códigos QR, verificar el estado de la sesión y enviar mensajes individuales (con imágenes opcionales) a contactos de WhatsApp. Todo desde una interfaz web intuitiva.

---

## Funcionalidades ✨

- **Generación de QR** para cada sesión de WhatsApp Web.
- **Verificación de sesión**: Comprueba si tu sesión está activa o desconectada.
- **Envía mensajes de texto** o **imágenes** a números específicos.
- **Reinicia sesiones**: Recarga la sesión y genera un nuevo QR si se desconecta.

---

## Instalación 🔧

1. **Clona el repositorio**:

2. **Instala las dependencias**:

   ```bash
    npm install
    ```

3. **Inicia el servidor**:

    ```bash
    node server.js
    ```

4. **Accede a la interfaz**:

    Abre tu navegador y visita http://localhost:3000 para ver la interfaz de usuario.

 ## Estructura del Proyecto 📂

        /tu-proyecto
        ├── /public             # Archivos estáticos (HTML, CSS, JS)
        ├── /uploads           # Carpeta para almacenar imágenes subidas
        ├── /tokens            # Sesiones de WhatsApp Web
        ├── /src               # Lógica principal (wppManager.js)
        ├── /node_modules      # Dependencias de Node.js
        ├── package.json       # Archivo de configuración de dependencias

## Flujo de la Aplicación 💻

1. **Generar QR para sesión** 📲
- Cuando seleccionas una sesión, puedes generar un código QR para escanear con tu aplicación de WhatsApp. Esto permite iniciar sesión en WhatsApp Web y sincronizar el chat.

2. **Verificar estado de sesión** ✅
- Puedes comprobar si una sesión está conectada o desconectada. Si la sesión está inactiva, te pedirá que la reinicies generando un nuevo QR.

3. **Enviar mensajes** 📩
- Envía mensajes de texto o imágenes a números de WhatsApp especificados. Esto se hace a través de un formulario en el frontend. También se permite adjuntar una imagen en el mensaje.

4. **Reiniciar sesión** 🔄
- Si la sesión está desconectada o presenta errores, puedes reiniciarla, lo que elimina cualquier sesión previa y genera un nuevo QR para iniciar sesión.
